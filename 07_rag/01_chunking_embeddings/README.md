# Chunking and embeddings

Legend: `[ ]` not started · `[*]` in progress · `[x]` done

* [ ] Split markdown/PDF text with overlap
* [ ] Embed 20 chunks; cosine between related vs unrelated
* [ ] Store `source` metadata on every chunk
* [ ] Never embed the whole book as one vector

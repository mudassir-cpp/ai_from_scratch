# Networks and backprop

Legend: `[ ]` not started · `[*]` in progress · `[x]` done

* [ ] `nn.Module` with two `Linear` layers
* [ ] ReLU, sigmoid, tanh — vanishing vs dead ReLU
* [ ] `CrossEntropyLoss` (logits in, no softmax in forward)
* [ ] Parameter count: `sum(p.numel() for p in model.parameters())`
* [ ] Sketch backprop for a 2-layer net on paper once

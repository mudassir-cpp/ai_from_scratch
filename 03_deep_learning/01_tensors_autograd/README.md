# Tensors and autograd

Legend: `[ ]` not started · `[*]` in progress · `[x]` done

* [ ] Create tensors, dtypes, `to(device)`
* [ ] Broadcasting rules (same as NumPy)
* [ ] Matmul `x @ w`
* [ ] `requires_grad=True`, `loss.backward()`, inspect `.grad`
* [ ] `detach()` vs `no_grad`
* [ ] Implement `y = wx + b` MSE backward and check vs autograd

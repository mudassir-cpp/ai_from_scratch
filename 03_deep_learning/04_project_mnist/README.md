# Project — MNIST digit classifier

**Ship a PyTorch MLP that you trained, logged, and can re-run.**

Legend: `[ ]` not started · `[*]` in progress · `[x]` done

## Build

* [ ] `src/model.py` — MLP
* [ ] `src/train.py` — loop, val split, checkpoints
* [ ] Flatten 28×28 → hidden → 10 logits
* [ ] Test accuracy ≥ 90% (aim 97%+ if you have time)
* [ ] `results/` loss + acc curves
* [ ] 8 sample predictions (correct + wrong)

## Success

* [ ] `requirements.txt` + README run commands
* [ ] One paragraph: what LR / width / epochs you used and why

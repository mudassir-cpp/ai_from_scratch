# Docker

Legend: `[ ]` not started · `[*]` in progress · `[x]` done

* [ ] Python slim base image
* [ ] Copy only what you need; multi-stage if weights are huge
* [ ] `docker run -p 8000:8000`
* [ ] Healthcheck endpoint

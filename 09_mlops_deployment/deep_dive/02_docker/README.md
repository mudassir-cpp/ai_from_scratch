# Docker — deep dive

Legend: `[ ]` not started · `[*]` in progress · `[x]` done

## What to learn
* [ ] Dockerfile, .dockerignore
* [ ] Multi-stage for large weights (optional)
* [ ] Port mapping
## Mini drill
* [ ] `docker build && docker run` from README

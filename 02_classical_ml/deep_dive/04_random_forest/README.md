# Random forest — deep dive

Legend: `[ ]` not started · `[*]` in progress · `[x]` done

## What to learn

* [ ] Bagging: bootstrap samples
* [ ] Random feature subset per split
* [ ] Why variance drops vs a single tree
* [ ] `n_estimators`, `max_features`, OOB score
* [ ] ExtraTrees vs RF (optional)

## From scratch

* [ ] Average 5 of your tiny trees on bootstrap samples (hacky is fine)
* [ ] sklearn `RandomForestClassifier` as the real tool

## Applied

* [ ] When: strong tabular baseline, little tuning
* [ ] When not: huge sparse text (use linear / embeddings)

## Mini project

* [ ] Same dataset as logistic vs RF; table of F1 and training time

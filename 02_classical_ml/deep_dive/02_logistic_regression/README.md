# Logistic regression — deep dive

Legend: `[ ]` not started · `[*]` in progress · `[x]` done

## What to learn

* [ ] Sigmoid, log-odds, decision boundary
* [ ] Binary cross-entropy (not MSE for classification)
* [ ] Gradient of BCE w.r.t. weights
* [ ] Threshold 0.5 is a *choice*, not a law
* [ ] Multiclass: softmax / one-vs-rest intuition

## From scratch

* [ ] NumPy logistic regression + GD
* [ ] Accuracy + BCE on train vs val
* [ ] Match `sklearn.linear_model.LogisticRegression` on a tiny linearly separable set

## Applied

* [ ] When: baseline classifier, calibrated-ish probabilities, fast
* [ ] When not: XOR-style features unless you engineer them

## Mini project

* [ ] Binary labels, plot ROC, pick a threshold for high recall

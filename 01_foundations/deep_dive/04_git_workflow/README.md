# Git workflow — deep dive

Legend: `[ ]` not started · `[*]` in progress · `[x]` done

## What to learn

* [ ] `status`, `diff`, `log --oneline`
* [ ] Staging hunks vs whole files
* [ ] Branches, merge, resolve simple conflicts
* [ ] `.gitignore` for venv, data, weights
* [ ] Commit messages: problem → change → why

## From scratch

* [ ] 5 commits on the month project (not one dump)
* [ ] Feature branch → merge to main
* [ ] Revert one bad commit (or reset if unpushed)

## Applied

* [ ] When: always — your portfolio is Git history
* [ ] When not: committing `.env` or 2GB checkpoints

## Mini drill

* [ ] README change on a branch; PR-style summary in commit body

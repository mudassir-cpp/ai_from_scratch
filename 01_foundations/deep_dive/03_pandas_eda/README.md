# pandas EDA — deep dive

Legend: `[ ]` not started · `[*]` in progress · `[x]` done

## What to learn

* [ ] `read_csv`, dtypes, `info()`, `describe()`
* [ ] `loc` / `iloc`, filtering, `groupby`
* [ ] Missing values: count, fill, drop
* [ ] Merge/join intuition
* [ ] Export clean CSV/JSON

## From scratch

* [ ] EDA script: shape, nulls, numeric summary, top categories
* [ ] One plot saved to `results/`

## Applied

* [ ] When: every real dataset before modeling
* [ ] When not: streaming TB-scale data (use Spark/polars later)

## Mini drill

* [ ] Titanic or any CSV: 10-line summary printed + saved JSON report

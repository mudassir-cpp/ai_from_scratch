# Hugging Face

Legend: `[ ]` not started · `[*]` in progress · `[x]` done

* [ ] Load a pretrained classifier, run `pipeline("sentiment-analysis")`
* [ ] Tokenize a Dataset map
* [ ] Fine-tune 1–3 epochs on a small split first
* [ ] Compute metrics in `compute_metrics`
* [ ] Save model + tokenizer together

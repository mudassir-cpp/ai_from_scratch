# Attention and transformers

Legend: `[ ]` not started · `[*]` in progress · `[x]` done

* [ ] Attention formula: `softmax(QKᵀ / √d) V`
* [ ] Causal mask vs bidirectional
* [ ] Parameter count ballpark of a small BERT
* [ ] Why long context is expensive (quadratic attention)

Applied: you need this to debug “model ignored the negation” and context length costs — not to invent a new architecture.

# Quantization and ONNX

Legend: `[ ]` not started · `[*]` in progress · `[x]` done

* [ ] `torch.onnx.export` a small model
* [ ] ORT latency vs PyTorch
* [ ] Quantize; measure accuracy drop
* [ ] Document machine (CPU model / GPU)

# LoRA / QLoRA

Legend: `[ ]` not started · `[*]` in progress · `[x]` done

* [ ] Adapter vs full weights
* [ ] Train LoRA on a tiny instruction JSONL
* [ ] Merge or load adapter at inference
* [ ] If no GPU: use Colab / small model (e.g. 1–3B) or API fine-tune
* [ ] Never claim “7B on laptop” if you didn’t; document the box
